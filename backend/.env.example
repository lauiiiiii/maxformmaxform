## MySQL 连接配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=survey_system

## JWT 签名密钥（务必在生产环境更换为强随机值）
JWT_SECRET=please-change-me

## JWT 过期时间（示例：7d、12h、3600s）
JWT_EXPIRES_IN=7d

## 前端地址（用于 CORS 白名单）
FRONTEND_URL=http://127.0.0.1:63000

## 后端监听端口
PORT=63002
