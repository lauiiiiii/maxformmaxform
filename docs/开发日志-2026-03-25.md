# 开发日志 - 2026-03-25

## 目标

今天的工作目标有三项：

1. 扩展 `scripts/system-smoke.mjs`，把 2026-03-24 新增能力纳入真实系统冒烟。
2. 用真实 MySQL 开发库跑出今天的绝对通过数，替换旧的 `31 / 31` 口径。
3. 重新验证前端 npm 冒烟入口、后端自动化测试和前端构建，形成 2026-03-25 的测试归档。

## 完成内容

### 1. 重写系统冒烟脚本

修改文件：

- `scripts/system-smoke.mjs`

本次调整内容：

- 将旧脚本中不可读的结果名替换为可读的 ASCII 检查项名称。
- 统一封装 JSON 请求、multipart 上传、二进制下载三类调用。
- 保留原有认证、部门、成员导入、文件夹、问卷发布、消息、审计、回收站链路。
- 新增职位管理链路：
  - 创建职位
  - 查询职位列表
  - 更新职位
  - 删除职位
- 新增上传题链路：
  - 公开上传成功
  - `maxFiles` 超限拦截
  - 错误 upload token 拦截
  - 错误题号绑定拦截
- 新增答卷管理链路：
  - 查询答卷列表
  - 导出问卷 Excel
  - 打包下载附件 zip
- 新增结果统计断言：
  - 汇总指标
  - 单选统计
  - 评分统计
  - 上传题统计
  - 设备 / 浏览器 / 操作系统统计
  - 地域空态结构

结果：

- 系统冒烟检查项从 `31` 扩展到 `55`。
- 仓库根目录和前端 npm 入口都能稳定执行同一套更新后的脚本。

### 2. 明确上传题提交流程口径

本轮联调中进一步收敛了上传题提交流程：

- 上传题在提交答卷时，客户端只需要传 `id / uploadToken`。
- `name / type / size / url` 不再要求前端回填。

本次处理方式：

- 后端先解析 `submissionToken + id + uploadToken` 对应的真实文件记录。
- 再基于服务端文件信息做数量、大小、类型与题目绑定校验。
- 系统冒烟中的正向上传题提交流程同步收敛为最小文件引用：
  - `id`
  - `uploadToken`

当前判断：

- 上传题提交契约已经收敛到最小可用集合，服务端和冒烟脚本已对齐。
- 后续重点变为保持文档、第三方调用方与自动化脚本口径一致。

### 3. 重新验证 2026-03-25 真实结果

今天实际执行结果如下：

1. 仓库级系统冒烟
   - 命令：`node scripts/system-smoke.mjs`
   - 结果：通过
   - runId：`20260325060823`
   - `55 / 55` 通过
2. 前端 npm 冒烟入口
   - 命令：`cmd /c npm run smoke:system`
   - 结果：通过
   - runId：`20260325060946`
   - `55 / 55` 通过
3. 后端自动化测试
   - 命令：`cmd /c npm test`
   - 结果：通过
   - `16 / 16` 通过
4. 前端生产构建
   - 命令：`npm.cmd run build`
   - 结果：通过
   - `vue-tsc --noEmit` 通过
   - `vite build` 通过
5. Playwright 入口
   - 命令：`cmd /c npm run test:e2e`
   - 结果：未通过
   - 返回：`Error: No tests found`

## 今日结论

- 当前系统冒烟口径已更新为 `55 / 55`，旧的 `31 / 31` 已不再代表当前仓库能力。
- 新增的职位管理、公开上传、上传题校验、答卷下载、附件打包、结果统计增强，已经进入可复用的真实联调脚本。
- 当前后端自动化测试数量为 `16 / 16`，已包含“上传题最小文件引用正向提交”回归用例。
- 当前主要测试缺口仍是浏览器级 E2E，而不是后端接口或前端构建。

## 当前遗留问题

- `Playwright` 入口仍无实际用例文件。
- 上传题提交契约已收敛为最小文件引用，仍需持续同步文档与调用方。
- `vite build` 仍有大 chunk 警告，但不影响本轮功能验收。
- 地域统计正向数据链路尚未稳定落地到主数据库结构。

## 下一步建议

1. 为结果页、答卷管理页、下载链路补第一批 Playwright 用例。
2. 持续同步文档、脚本和外部调用方，统一按 `id + uploadToken` 接入上传题提交。
3. 明确省市来源采集方案，再补地域统计正向回归。
4. 在后续前端拆包优化后，再补一轮构建与加载回归。
