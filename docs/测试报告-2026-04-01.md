# 测试报告 - 2026-04-01

## 1. 结论

截至 `2026-04-01`，本仓库已补齐根级统一测试入口，`quick`、`smoke` 与 `full` 都可直接在仓库根目录执行；当前 backend、build、双 smoke 与 Playwright 浏览器级 E2E 均已通过。

本轮最重要的变化：

- 新增根级 `package.json`，可直接使用 `npm test`、`npm run test:smoke`、`npm run test:e2e`、`npm run test:all`
- 新增 `scripts/test-suite.mjs`，统一编排后端单测、前端构建校验、双 smoke 入口与 E2E
- 根级包声明 `"type": "module"`，消除了 `shared/*.js` 的 `MODULE_TYPELESS_PACKAGE_JSON` 警告

## 2. 测试环境

- 测试日期：`2026-04-01`
- 项目目录：`e:\maxform\maxformmaxform`
- 时区：`Asia/Hong_Kong` / `+08:00`
- 数据库：`127.0.0.1:3309 / survey_system`
- 说明：本次结果基于当前工作区实际状态，包含未提交中的业务改动

## 3. 执行结果

### 3.1 根级 quick 入口

执行目录：

- `maxformmaxform/`

执行命令：

```bash
node scripts/test-suite.mjs quick
```

执行结果：

- 通过
- 后端单测阶段：`169 / 169` 通过
- 前端构建阶段：`vue-tsc --noEmit` 与 `vite build` 通过
- 编排耗时摘要：`backend=pass@41.8s, frontend-build=pass@53.6s`

### 3.2 后端单测

执行目录：

- `backend/`

执行命令：

```bash
cmd /c npm test
```

执行结果：

- 通过
- `169 / 169` 通过
- `# fail 0`
- `duration_ms 46816.5772`

本轮确认点：

- 管理 AI 执行台账与导出接口测试已纳入后端回归
- 系统配置读写与连通性探测测试已纳入后端回归
- 根级 `package.json` 补齐后未再出现 `MODULE_TYPELESS_PACKAGE_JSON` 警告

### 3.3 根级 smoke 入口

执行目录：

- `maxformmaxform/`

执行命令：

```bash
node scripts/test-suite.mjs smoke
```

执行结果：

- 通过
- repo-root smoke：`64 / 64`
- frontend smoke：`64 / 64`
- 编排耗时摘要：`smoke-root=pass@27.0s, smoke-frontend=pass@23.0s`

本轮 runId：

- repo-root：`1775027686870-18952-gtu9su`
- frontend smoke：`1775027714413-15604-6xsjiv`

补充观察：

- 两个 smoke 入口仍可复用独立端口与独立临时测试库策略
- `SMOKE_SHARED_DB=1` 与 `SMOKE_KEEP_DB=1` 的行为未被本轮脚本改造破坏

### 3.4 浏览器级 E2E

执行目录：

- `maxformmaxform/`

执行命令：

```bash
node scripts/test-suite.mjs e2e
```

执行结果：

- 通过
- `20 / 20` 通过
- 总耗时约 `1.4m`

额外日志：

- Playwright WebServer 仍有 `util._extend` 废弃警告
- 前端编译仍有 `defineProps` / `defineEmits` 宏导入警告

### 3.5 根级 full 入口

执行目录：

- `maxformmaxform/`

执行命令：

```bash
node scripts/test-suite.mjs full
```

执行结果：

- 通过
- 后端 `169 / 169`
- 前端构建通过
- repo-root smoke `64 / 64`
- frontend smoke `64 / 64`
- Playwright `20 / 20`
- 编排耗时摘要：`backend=pass@42.6s, frontend-build=pass@59.2s, smoke-root=pass@21.3s, smoke-frontend=pass@21.4s, e2e=pass@86.3s`

## 4. 当前判断

- 根级测试入口已具备可用性，适合开发时优先从仓库根执行
- 当前 `npm run test:all` 可直接作为本地总回归入口
- 当前残留问题已从“测试失败”收敛为“测试警告日志仍需清理”

## 5. 建议

1. 先修复两条建卷后未跳转到 `/surveys/:id/edit` 的用例或页面行为，再继续扩展编辑器类 E2E。
2. 校准 `management-pages.spec.ts` 中游客重定向断言，确认 `/question-banks` 与 `/user-dashboard?tab=repo` 哪个才是最新产品行为。
3. 为题库页和文件夹页补稳定的 `data-testid`，降低 UI 调整对 Playwright 的脆弱性。
4. 保持根级 `test-suite` 作为统一入口，避免继续扩散目录切换式命令文档。
