# 测试报告 - 2026-03-26

## 1. 测试结论

截至 `2026-03-26`，本轮验证结论如下：

- 系统联调冒烟通过
  - 仓库级入口 `node scripts/system-smoke.mjs`
  - `56 / 56` 通过
  - runId: `20260326070109`
- 前端 npm 冒烟入口通过
  - `cmd /c npm run smoke:system`
  - `56 / 56` 通过
  - runId: `20260326070038`
- 后端自动化测试通过
  - `cmd /c npm test`
  - `18 / 18` 通过
- 前端生产构建通过
  - `npm.cmd run build`
  - `vue-tsc --noEmit` 通过
  - `vite build` 通过
- Playwright 入口仍未形成可执行用例
  - `cmd /c npm run test:e2e`
  - 返回 `Error: No tests found`

综合判断：

- 当前仓库可通过“系统冒烟 + 后端自动化测试 + 前端构建”完成 2026-03-26 这一轮联调验收。
- `matrix multi / matrix dropdown / ratio` 已经进入真实提交流程与结果统计覆盖。
- 结果页真实图表、附件下载和 SPSS 语法导出已进入可构建状态。
- 当前最大缺口仍是浏览器级 E2E 回归，而不是接口或构建阻塞。

## 2. 测试环境

- 测试日期：`2026-03-26`
- 项目目录：`d:\QQFile\maxform\maxformmaxform`
- 时区：`Asia/Hong_Kong` / `+08:00`
- 后端冒烟端口：`63102`
- 前端允许来源：`http://127.0.0.1:63000`
- 数据库：
  - Host：`127.0.0.1`
  - Port：`3309`
  - Database：`survey_system`
  - User：`root`

说明：

- 系统冒烟使用真实 MySQL 开发库，不是 mock 环境。
- 冒烟脚本会创建带时间戳的临时账号、部门、职位、文件夹、问卷、答卷和上传文件，并在流程尾部清理本轮测试数据。

## 3. 执行命令与结果

### 3.1 仓库级系统冒烟

执行目录：

- `maxformmaxform/`

执行命令：

```bash
$env:DB_HOST='127.0.0.1'
$env:DB_PORT='3309'
$env:DB_USER='root'
$env:DB_PASSWORD='123456'
$env:DB_NAME='survey_system'
$env:FRONTEND_URL='http://127.0.0.1:63000'
$env:PORT='63102'
node scripts/system-smoke.mjs
```

执行结果：

- 通过
- runId：`20260326070109`
- `56 / 56` 通过

### 3.2 前端 npm 冒烟入口

执行目录：

- `maxformmaxform/frontend/`

执行命令：

```bash
cmd /c npm run smoke:system
```

执行结果：

- 通过
- runId：`20260326070038`
- `56 / 56` 通过

说明：

- `frontend/package.json` 暴露的 npm 冒烟入口，已能稳定调用更新后的 `../scripts/system-smoke.mjs`。
- 本轮并行触发两套 smoke 时发现 `runId` 存在秒级冲突风险；顺序执行时两套入口均稳定通过。

### 3.3 后端自动化测试

执行目录：

- `maxformmaxform/backend/`

执行命令：

```bash
cmd /c npm test
```

执行结果：

- 通过
- `18 / 18` 通过

当前自动化覆盖重点：

1. 删除存在子部门的部门应被拒绝。
2. 删除部门时应清理成员 `dept_id`。
3. `POST /api/users/import` 应正确返回 `created / skipped / errors`。
4. `GET /api/positions` 返回职位列表。
5. `POST /api/positions` 正确拦截重复编码。
6. 删除问卷进入回收站，而不是直接物理删除。
7. 清空回收站时联动删除答卷。
8. `GET /api/surveys/:id/results` 返回题目级统计。
9. `GET /api/surveys/:id/results` 聚合已有地域数据。
10. `POST /api/answers/download/attachments` 返回 zip 附件包。
11. `POST /api/surveys/:id/uploads` 允许已发布问卷公开上传。
12. 上传数量超过 `maxFiles` 时应被拒绝。
13. 上传题提交时使用错误 upload token 应被拒绝。
14. 上传题提交时支持矩阵多选与矩阵下拉正向通过。
15. 比重题答案总和为 `100` 时可成功通过。
16. 上传题提交时使用最小文件引用 `id + uploadToken` 可成功通过。
17. 上传题提交超过 `maxFiles` 应被拒绝。
18. 上传文件绑定到错误题号时应被拒绝。

### 3.4 前端生产构建

执行目录：

- `maxformmaxform/frontend/`

执行命令：

```bash
npm.cmd run build
```

执行结果：

- 通过
- `vue-tsc --noEmit` 通过
- `vite build` 通过

补充说明：

- 本轮构建已启用更细的 `manualChunks` 拆分。
- 构建输出未再出现此前那类显著的大 chunk 告警。

### 3.5 Playwright 入口检查

执行目录：

- `maxformmaxform/frontend/`

执行命令：

```bash
cmd /c npm run test:e2e
```

执行结果：

- 未通过
- 错误：`Error: No tests found`

结论：

- `Playwright` 依赖与 npm 入口仍在。
- 仓库内当前依旧没有实际 E2E 用例文件。

## 4. 本轮系统冒烟新增覆盖

本轮系统冒烟从旧口径 `55` 项扩展到 `56` 项，新增覆盖的核心点为：

1. 分析问卷中新增比重题样本提交。
2. 结果统计中新增比重题断言：
   - `brand` 平均占比 `45`
   - `price` 平均占比 `35`
   - `service` 平均占比 `20`

本轮 `56` 项系统检查全部通过，覆盖的大类能力如下：

1. 健康检查
2. 管理员登录与身份读取
3. 普通用户注册与身份读取
4. 部门创建、删除与成员归属清理
5. 职位创建、查询、更新、删除
6. 成员导入与读取
7. 父子文件夹创建与删除保护
8. 问卷创建、移动、发布、公开读取
9. 上传题公开上传与负向校验
10. 问卷答卷提交
11. 答卷列表、Excel 导出、附件包下载
12. 结果统计读取与题目级断言
13. 消息读取与已读标记
14. 审计日志读取
15. 回收站移动、查询、恢复、清空

## 5. 当前未覆盖项与风险说明

虽然本轮系统冒烟、后端自动化测试和前端构建均已通过，但以下范围仍未形成完整覆盖：

- 浏览器级 UI 自动化回归
  - 当前 `npm run test:e2e` 无可执行用例
- 结果页图表切换、时间筛选、附件下载、SPSS 导出等真实交互
  - 页面能力已到位，但浏览器自动化仍缺失
- 地域统计正向链路的系统级验证
  - 后端测试已覆盖聚合逻辑，但主数据库尚未稳定写入省市来源数据
- 并行 smoke 入口冲突
  - 当前 `runId` 基于秒级时间戳，两个入口若在同一秒触发，可能出现临时用户名冲突

## 6. 建议的下一步

1. 调整 `scripts/system-smoke.mjs` 的 `runId` 生成策略，避免并行入口冲突。
2. 为结果页图表、时间筛选、Excel 导出、附件包下载、SPSS 语法导出补第一批 Playwright 用例。
3. 明确答卷来源地采集方案，再补地域统计正向链路。
4. 继续把题型注册、填写渲染、统计适配收口到共享题型模型。
