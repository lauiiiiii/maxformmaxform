# PR 摘要 - 2026-04-01 测试脚本统一与文档补齐

## 变更主题

本轮变更聚焦三件事：

1. 在仓库根新增统一测试入口，降低目录切换和命令记忆成本。
2. 修复创建草稿与浏览器回归失配问题，恢复 E2E 全绿。
3. 将 README、开发指南、测试报告、开发日志、PR 文档刷新到当前真实状态。

## 主要改动

### 1. 根级测试入口

新增：

- `package.json`
- `scripts/test-suite.mjs`

能力：

- `npm test` -> quick 校验
- `npm run test:smoke` -> 双 smoke 入口
- `npm run test:e2e` -> Playwright
- `npm run test:all` -> backend + build + smoke + e2e

### 2. 草稿创建与校验修复

更新：

- `backend/src/services/surveyCommandService.js`
- `backend/src/routes/surveys.js`
- `backend/test/surveys.command.routes.test.js`

处理：

- 创建、更新、`validate`、`dry-run` 路径允许空题草稿
- 发布路径仍保留“至少一题”的业务约束
- 新增后端测试覆盖空题草稿创建

### 3. Playwright 用例修复

更新：

- `frontend/e2e/management-pages.spec.ts`
- `frontend/e2e/survey-pages.spec.ts`

处理：

- 校准游客访问管理页的 redirect 断言
- 适配题库管理当前的弹窗/页面结构
- 文件夹导航改为按文案定位
- 题库存档断言补齐 `AI生成` 标签
- 确认框交互改为直接点击主按钮，降低文案依赖

### 4. 文档刷新

更新：

- `README0326.md`
- `docs/开发指南.md`
- `backend/README.md`

新增：

- `docs/测试报告-2026-04-01.md`
- `docs/开发日志-2026-04-01.md`
- `docs/PR-2026-04-01-测试脚本统一与文档补齐.md`

## 验证结果

- `node scripts/test-suite.mjs quick`：通过
  - 后端 `169 / 169`
  - 前端构建通过
- `node scripts/test-suite.mjs smoke`：通过
  - repo-root smoke `64 / 64`
  - frontend smoke `64 / 64`
- `node scripts/test-suite.mjs e2e`：通过
  - `20 / 20` 通过
- `node scripts/test-suite.mjs full`：通过
  - backend、build、双 smoke、E2E 全部通过

## 当前风险与后续

- 当前风险
  - WebServer 启动日志中仍有 `util._extend` 与 `defineProps` / `defineEmits` 警告

- 建议 follow-up
  - 清理 Playwright WebServer 启动警告
  - 给题库页与文件夹页继续补更稳定的 `data-testid`
  - 继续把新增业务流接入根级 `test-suite`
